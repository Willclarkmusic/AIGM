# Backend Environment Variables

# Database
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/aigm

# Redis
REDIS_URL=redis://localhost:6379

# CORS
CORS_ORIGINS=http://localhost:3000,https://localhost:3000

# Auth0 Configuration (Keep secrets in AWS Secrets Manager)
AUTH0_DOMAIN=your-domain.auth0.com
AUTH0_CLIENT_ID=your-client-id
AUTH0_AUDIENCE=https://your-api-identifier
# AUTH0_CLIENT_SECRET - Keep in Secrets Manager

# Ably Configuration
# ABLY_API_KEY - Keep in Secrets Manager

# AWS Configuration
AWS_REGION=us-east-1
# AWS_ACCESS_KEY_ID - Keep in Secrets Manager
# AWS_SECRET_ACCESS_KEY - Keep in Secrets Manager

# S3/File Storage
S3_BUCKET_NAME=aigm-files-dev
# CLOUDFLARE_R2_SECRET_KEY - Keep in Secrets Manager

# Email Configuration
FROM_EMAIL=noreply@aigm.world
AWS_SES_REGION=us-east-1

# Security
# JWT_SECRET_KEY - Keep in Secrets Manager

# Development
DEBUG=true
LOG_LEVEL=INFO